#include "main.h"
#include <stdio.h>

/**
 * main - check the code for ALx School students.
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
	printf("%d\n", _strlen("ALx"));
	return (EXIT_SUCCESS);
}
